[project]
name = "euvox-mod-rendering"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "euvox-common-schemas",
    "euvox-operation-registry",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/euvox"]
